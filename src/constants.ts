const DEFAULT_SLIDER_PARAMS = { isVertical: false, showTooltips: true, withMarkup: false };

const DEFAULT_SLIDER_CLASS = 'liquid-slider';

const HANDLER_PAIR_OPTIONS = new Map()
  .set(null, null)
  .set('start', false)
  .set('end', true);

export { DEFAULT_SLIDER_CLASS, DEFAULT_SLIDER_PARAMS, HANDLER_PAIR_OPTIONS };
