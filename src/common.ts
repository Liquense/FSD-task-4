const defaultSliderClass = "liquidSlider";

/**
 * Оборачивает функцию в такую, что после её выполнения будет запущена подписавшаяся функция
 * @param eventFunction функция, на выполнение которой происходит подписка
 * @param subscriber функция-подписчик
 * @param arguments аргументы функции-события
 */
function addHandler(eventFunction: Function, subscriber: Function, arguments: typeof Array) {

}
