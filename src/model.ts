export default class Model {
    constructor(parameters:
                    {
                        min?: number;
                        max?: number;
                        items?: Array<string>;
                    }
    ) {
    }
}
